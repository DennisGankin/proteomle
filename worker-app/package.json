{
  "name": "protl-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:data": "uv run python tools/export_dataset.py",
    "build:target-pool": "python tools/build_target_pool.py"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "wrangler": "^4.37.1",
    "@cloudflare/workers-types": "^4.20250321.0"
  }
}
